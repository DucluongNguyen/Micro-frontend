// Superseded by Overview.tsx + Stats.tsx (this remote now owns nested
// routes internally - see App.tsx). Left as a re-export instead of deleted
// because this environment can't remove files from this mount; nothing
// imports from this path.
export { default } from './Overview';
